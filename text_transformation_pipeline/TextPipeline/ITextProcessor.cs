
// This is the interface for the text processing pipeline.
// The process method takes an input string and returns a processed string.

public interface ITextProcessor
{
    string Process(string input);
}